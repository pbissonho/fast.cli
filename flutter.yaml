name: myapp7
version: 1.0.0+1

dependencies:
  cupertino_icons: ^0.1.2
  flutter:
    sdk: flutter
  flutter_bloc:

dev_dependencies:
  flutter_test:
    sdk: flutter
  koin_test:

environment:
  sdk: ">=2.1.0 <3.0.0"

flutter:
  uses-material-design: true
