

void main() {
  /*
  test('Shoud create a Flutter Project', () async {
    var appName = 'tutuca';

    var args = FlutterProjectArgs(
        useAndroidX: false,
        name: appName,
        description: 'vem_tutuca',
        useKotlin: false,
        useSwift: false);

    var createrFlutter = CreaterFlutterAction(appName, args, TenazProcess());

    await createrFlutter.execute();

    var createStructAction = CreateProjectStructure(
        '$appName/lib', yamlManager.structure.mainFolder);
    await createStructAction.execute();
  });

 
  test('Exist test', () async {
    var data = await Directory('$path/lib').existsFiles();
    expect(false, data);
  });

  test('path test', () async {
    await Directory('/teste/tutuca').createRecursive();
  });*/
}
